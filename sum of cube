def sum_of_cubes(m, n):
    if m > n:
        return 0
    total = sum(i**3 for i in range(m, n + 1))
    return total

# Test cases
print(sum_of_cubes(2, 10))  # Sum of cubes from 2 to 10
print(sum_of_cubes(6, 6))   # 6^3 = 216
print(sum_of_cubes(10, 2))  # 0, because m > n
